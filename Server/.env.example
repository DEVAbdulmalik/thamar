# MySQL connection
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=student_db

# JWT
# Generate a strong random value, e.g.: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# Google Cloud Speech-to-Text service account key file path
GOOGLE_APPLICATION_CREDENTIALS=key.json

# Server
PORT=3001
